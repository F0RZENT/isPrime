function isPrime(x){
    if(x <= 1){
        return false;
    }
    for (let i = 2; i < x; i++) {
        if (x % i == 0) {
            return false;
        }
    }
    return true;
}
isPrime(28);
function getPrimes(limit) {
    let i = 2;
    while(i <= limit){
        for(let i = 2; i < limit; i++){
            
        }
    }
    return
}